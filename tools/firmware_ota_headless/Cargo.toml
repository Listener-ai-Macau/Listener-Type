[package]
name = "listener-firmware-ota-headless"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt", "macros", "time"] }
denzic-ota-core = { path = "../../third_party/denzic-platform/ota/host/rust" }
