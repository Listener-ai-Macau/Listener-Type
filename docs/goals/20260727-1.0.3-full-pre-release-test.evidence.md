# Evidence：1.0.3 发布前全量测试（先测后发）

日期：2026-07-27  
树：`5ade380`（goal 文档已入库；门禁在含 gate 路径修复的 master 上跑通）  
Artifact：`.cache/validation/full-pre-release-20260727-222719/`  
指针：`.cache/validation/full-pre-release-latest.txt`

## Goal 结果：**PASS（机器全绿）**

| # | 检查 | 结果 | 证据 |
|---|---|---|---|
| 1 | `npm run release:check` | **PASS** exit 0 | `release-check.log` + `release-check.exit` = `0` |
| 2 | `check:module-budgets` + softOver4500 | **PASS** `softOver4500: []` | `module-budgets.log` |
| 3 | `check:embedded-ble-processing-led` | **PASS** | `led.log` |
| 4 | `cargo test --lib`（含于 release:check） | **885 passed; 0 failed; 18 ignored** | `release-check.log` |
| 5 | `npm run verify` + `npm test` | **PASS** | `verify.log`, `npm-test.log` |
| 6 | 真机 OTA package / BLE preflight / wired / runtime smoke | **PASS** | 见下 |
| 7 | 本证据文件 | **存在** | 本文件 |

## 明细

### A. `release:check`

- 环境：`LISTENER_TYPE_DISABLE_BACKGROUND_BLE=1`（避免后台 BLE 与 headless 抢门控）。
- dirty inventory / guard commits / hygiene / version **1.0.3** / operator notes / preproduction bench / recording+ASR latency / OTA speed contract / performance baselines：**PASS**
- frontend verify（含 unit tests + vite build）：**PASS**
- MSI same-version contract / updater 相关：随 release:check **PASS**
- `cargo test --lib -- --test-threads=1`：**885 ok / 0 fail / 18 ignored**（~27.5s）
- firmware_ota_headless：**20 passed**
- 收尾：`PASS: Listener Type release checks completed.`

### B. 预算与 LED（并行）

- MODULE BUDGET PASS；最大生产模块在 hard budget 内；**softOver4500 = []**
- processing-led 契约：**PASS**

### C. 前端

- `verify`：demo copy / tsc / brand / dark-mode / unit tests / vite build 全 OK  
- `npm test`：rules / capsule / layout / device-health / settings-mock / BLE recovery UI / first-run / provider / demo / firmware-ota / marketplace：**全绿**

### D. 真机（当前 debug 二进制代理，非旧 MSI）

| 步骤 | 结果 | 要点 |
|---|---|---|
| OTA package check | PASS | 1.0.3 schema v2；sha256 `34d4f652…`；packageValid |
| BLE OTA preflight | PASS | 设备 **Blistener** `D80E2503DD55`；fw **1.0.3**；battery **100**；blockers `[]` |
| Wired COM3 check | PASS | 工厂包来自 `ListenerFirmware_1.0.3_ota.zip` |
| `windows-runtime-smoke.ps1` | PASS | 进程响应 + hotkey install；vault 无 ASR/LLM 凭据 → 不测真实转写 |

## 包与根目录（仍阻塞「对外安装包」）

| 位置 | 文件 | 备注 |
|---|---|---|
| `Listener/` | MSI 15 036 416 (2026-07-25)、OTA zip (2026-07-25) | **早于** 当前 master 重构/gate 修复 |
| `Listener/releases/1.0.3/` | MSI 13 914 112 (2026-07-26) + portable zip | 仍非当前 HEAD 构建 |
| `Denzic/` 根 | 本机未放齐 Type MSI + OTA 的标准根布局 | 发布合同要求根目录同名 |

**不得**把 7/25–7/26 旧 MSI 当作「当前树 1.0.3 最终安装包」。

## 已知非阻塞

- 无 vault 凭据时 runtime smoke 不验证真实 ASR 文本（日志已 warn）。
- 无人说话听写空转写：预期。
- 不自动 GitHub Release / 商店上传（Goal 人工边界）。

## Go / No-Go

| 维度 | 判定 |
|---|---|
| **机器全量测试 / Goal** | **GO** |
| **当前树可按 1.0.3 进入打包** | **GO** |
| **可对外分发安装包** | **NO-GO** — 须用当前树重打 MSI，与 OTA zip 一并落到 Denzic 根，再跑 `check:release-root-artifacts` |

一句话：**发布前测试 Goal 已 PASS；代码门禁绿；安装包尚未从当前树重打，故暂不宣称可发布安装物。**

## 建议下一步（打包，不在本 Goal 强制内）

1. `scripts/windows-package-msvc.ps1`（或项目约定）用 **当前 HEAD** 打 Type MSI  
2. 复制 MSI + `ListenerFirmware_1.0.3_ota.zip` → Denzic 根标准文件名  
3. `npm run check:release-root-artifacts`  
4. 可选：同版本 MSI 覆盖安装 smoke  
5. 再开发布/上传 Goal（若需要）
