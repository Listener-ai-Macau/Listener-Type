# Listener 1.0.4 冻结基线自动防回退

**状态：** release candidate
**日期：** 2026-08-04
**冻结身份：**
- Type source identity：the commit currently referenced by release tag `v1.0.4`
- Firmware commit/tag：`95aca299ab28b1970773c29864fa01c71fd8dbc6` / `v1.0.4`
- MSI SHA-256：`A7DFA087076DF7691ACB33E78360D16A9D01EB8AACB1FA437EC32FF46DB5E26F`
- OTA SHA-256：`D36552F61932CB7EE2966FBEB3CF85ED020F263D24B11F2B71AA30C29088DE2B`
- 人工通过记录：`Listener-Type/.artifacts/listener-1.0.4-endpoint-calibration-20260804`

## 目标

建立统一的 Listener 1.0.4 回归入口与诊断包，使之后任何改动都能自动发现录音、唤醒、多人过滤、BLE、OTA、灯效与默认设置回退；机器闭环先于人工体验。

## 技术指标

| 功能 | 成功状态 | 技术阈值 | 自动判定 |
|---|---|---|---|
| 发布身份 | 根目录 MSI/OTA 与冻结 SHA、源码 tag 一致 | MSI=`A7DF…E26F`，OTA=`D365…DE2B`；Type HEAD 为 `v1.0.4`，Firmware tag 仍指向冻结固件提交 | `verify-listener-1.0.4-identity` PASS |
| 唤醒边界 | 仅非 idle 且蓝牙灯亮可唤醒 | 源码/静态合同保持 idle 关闭语音监测 | 静态合同 + 既有 power/wake 测试 PASS |
| 唤醒确认 | 保留 800 ms 推测确认与当前灵敏度 | `LOCAL_CONFIRMATION_START_MS == 800`；不改 KWS 阈值 | 源码合同 + dictation_tests PASS |
| 胶囊可见 | 胶囊可见前不自动结束 | 自动结束依赖正文/目标说话人权威 | 源码合同 + 端点测试 PASS |
| 自动结束 | 正文后目标说话人停约 1000 ms 才结束；他人说话不续录、不拖长结束；空 final 保留账本 | `EMBEDDED_TARGET_SPEAKER_END_TIMEOUT_MS == 1000` | 源码合同 + dictation/volcengine 测试 PASS |
| 顺滑预览 | optimistic 流式上屏、burst≤160ms、同 session 空 payload 不抹字 | `smooth_preview_*` 门 + PREVIEW_BURST_REVEAL | `verify-listener-1.0.4-protected-contracts.mjs` + `npm run test:rules` PASS |
| 多人过滤 | 只保留唤醒者正文 | NonTarget 不能续录/污染 | 既有 speaker/endpoint 测试 PASS |
| 手动/声纹/自定义词/原始灯电平 | 功能不回退 | 既有设置与 meter 合同绿 | 相关静态/单元 PASS |
| 默认输入 | Type 默认 BLE OTA/嵌入式听写源 | `dictation_input_source=EmbeddedBle` | 默认设置合同 PASS |
| OTA 吞吐 | 连续三次严格 >60 KiB/s | 零 offset recovery / ACL failure / OOM / WDT | 装机 OTA 门（硬件可选） |
| 温启动 BLE | notify-ready <3000 ms | 温启动路径 | `check-type-startup-reconnect-speed` 阈值合同 |
| 录音长稳 | ≥20 分钟无重启/WDT/丢包/会话断裂 | 长会话门 | 固件 long session（硬件可选） |
| 默认设置 | 除“听写后自动发送”关外，指定功能开 | `send_key_after_dictation=false` 等 | 默认设置源码合同 PASS |
| 生产数据隔离 | 测试不读写生产 `%APPDATA%\Listener Type` | 测试 data dir 在 temp 或 `LISTENER_TYPE_DATA_DIR` | clean-env 门 PASS |

## 机器闭环

- **输入/触发：** `pwsh -NoProfile -File Listener-Type/scripts/run-listener-1.0.4-regression.ps1`（可选 `-IncludeHardware`）。
- **可观察结果：** `.artifacts/listener-1.0.4-regression/<stamp>/summary.json` 与各门 log。
- **自动判定：** `summary.json` 顶层 `result` 为 `PASS` 且无 `FAIL` 门；FAIL 必须修复后重跑，不得口头通过。
- **诊断：** `collect-listener-1.0.4-diagnostics.ps1` 产出隐私边界内的诊断包。

## 人工边界

仅在全部机器判定 PASS 后，owner 才做：唤醒手感、双人实说、灯效视觉、Windows 配对弹窗。人工 FAIL 回到修复，不替代机器闭环。
