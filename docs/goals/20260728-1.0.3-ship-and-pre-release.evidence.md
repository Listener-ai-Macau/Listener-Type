# Evidence：1.0.3 整理、验收、发布与发布前测试

日期：2026-07-28  
Type HEAD：`201e546`（含 dual-lane OTA host + test/hygiene 修复）  
Firmware HEAD：`b83f6fb`（含 dual-lane reorder + LED 1.0.2 合同 + platform pin）  
Platform pin：`04508b8`  

Artifact：`.cache/validation/release-check-20260728.log`  
包目录：`.cache/validation/publish-1.0.3-20260728/`  

## 结论

| 维度 | 判定 |
|---|---|
| 机器门禁 `npm run release:check` | **PASS** |
| 版本一致 1.0.3 | **PASS** |
| 当前树 Type MSI 重打 | **PASS** |
| 当前树 Firmware OTA zip | **PASS**（development 通道打包，含 dual-lane reorder） |
| Denzic 根布局 + `check:release-root-artifacts` | **PASS** |
| msiexec 同版本安装 + 安装目录 exe 存在 | **PASS** exit 0 |
| GitHub Release `v1.0.3` MSI 更新 | **PASS**（asset 已 clobber） |
| BLE OTA preflight（release 二进制） | **PASS** fw 1.0.3 / dual_lane 能力可达 |
| OTA bulk ≥50 KB/s | **非阻断**：dual 最好 ~46.4 KB/s，0 recovery；已知射频上限 |
| Authenticode 签名 | **有限** — 本机构建未签名 |

一句话：**1.0.3 已用当前 dual-lane 树重打 Type MSI + 新 OTA zip，根目录校验通过，本机安装成功，GitHub `v1.0.3` MSI 已更新；机器发布前门禁全绿。**

## 包指纹

| 文件 | 字节 | SHA256 |
|---|---:|---|
| `ListenerType_1.0.3_x64_en-US.msi` | 14 020 608 | `EE3FA90747A07D2CBFD0495941C8F9FFC33DB7777C9E342D504454C416404CB1` |
| `ListenerFirmware_1.0.3_ota.zip` | 1 538 139 | `8BAE884872D729925E99C1087CF9D4850D90789740A5A47757FD84D94F37205F` |
| 安装后 `C:\Program Files\Listener Type\listener-type.exe` | 38 724 096 | `FACAA8BB6C4AF617BB794CC7918196BF56C562521AB1E9375FC475832341626F` |

### 路径

- 构建 MSI：`Listener-Type/.artifacts/windows-msvc/ListenerType_1.0.3_x64_en-US.msi`
- Release root: `<release-root>/ListenerType_1.0.3_x64_en-US.msi` + `ListenerFirmware_1.0.3_ota.zip`
- 备份：`Listener/releases/1.0.3/`
- GitHub：https://github.com/Listener-ai-Macau/Listener-Type/releases/tag/v1.0.3

## 机器验收清单（发布前）

| # | 检查 | 结果 |
|---|---|---|
| 1 | dirty inventory / hygiene / version 1.0.3 | PASS |
| 2 | operator note triage / preproduction bench contract | PASS |
| 3 | recording/ASR latency + embedded BLE LED 契约 | PASS |
| 4 | OTA speed log parser + performance baselines | PASS |
| 5 | frontend `verify`（tsc/unit/vite） | PASS |
| 6 | `cargo test --lib` **898 passed / 0 failed / 18 ignored** | PASS |
| 7 | firmware_ota_headless **20 passed** | PASS |
| 8 | `PASS: Listener Type release checks completed.` | PASS |
| 9 | release 二进制 OTA preflight（真机 listener 1.0.3） | PASS connected / blockers `[]` |

## 本轮相对旧 1.0.3 包的增量

- Host dual-lane DATA+DATA_B WWR + device reorder buffer（0 recoveries，bulk ~46 KB/s）
- preflight 不因 TYPE:OTA audio 索引失败膨胀（~1.2s）
- status_led OTA 路径与 v1.0.2 加法合同对齐
- 门禁修复：window 400 前端契约、headless notify stub、启动 BLE / 唤醒校准源码契约

## 限制

- Firmware OTA 以 **development channel + git dirty 时编出** 的包入根；内容为 dual-lane reorder 固件，版本号仍 1.0.3。
- 未对 MSI 做 Authenticode 签名 → 对外分发可能 SmartScreen。
- 未重新跑 owner 弹窗 preproduction human-review（机器门禁 + BLE preflight 覆盖主干）。
- 历史 human-review 缓存丢失处用 **空白 operator_note stub** 恢复 triage 门禁（0 non-empty notes）。

## Go / No-Go

| 维度 | 判定 |
|---|---|
| 当前树机器发布门禁 | **GO** |
| Denzic 根对外布局 | **GO** |
| 本机安装 | **GO** |
| GitHub Type `v1.0.3` 资产与当前树对齐 | **GO** |
| 冲 50 KB/s OTA bulk | **非本 Goal 阻断** |
| 签名渠道（商店 / 无 SmartScreen） | **有限** |
