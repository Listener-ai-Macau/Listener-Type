# 1.0.3 发布前测试与就绪结论

日期：2026-07-27  
树：`ad0940b`（含 release gate 路径修复）

## 结论（先说）

| 问题 | 答案 |
|---|---|
| **当前 master 机器发布门禁 `npm run release:check`？** | **PASS** |
| **磁盘上旧 MSI（7/26）能否直接当最终 1.0.3 发？** | **不建议** — 落后于 master 上 OTA/历史/重构后修复 |
| **能否以 1.0.3 发当前树？** | **可以发，但必须用当前树重新打 MSI**（同版本替换合同已有） |
| **Denzic 根目录 root 包检查？** | **FAIL（布置）** — 根目录还没有 `ListenerType_1.0.3_x64_en-US.msi` / 已有 zip 在 Listener 子目录 |

## 本机已跑通

### A. `npm run release:check`（完整）

PASS：dirty inventory、guard commits、hygiene、version 1.0.3、operator notes、preproduction bench、recording/ASR latency、OTA speed contract、performance baselines、frontend verify、MSI same-version contract、updater manifest test、**903 lib tests（1 fail 已修后全绿）**、firmware_ota_headless 20 tests。

过程中修掉的发布阻塞（拆模块后路径漂移）：

- `check-preproduction-bench-contract.mjs`
- `check-recording-low-latency-regression.mjs` / volcengine preview
- `check-performance-baselines.mjs` / cloud-services
- frontend `firmwareOta.test.ts` / `deviceSettingsMockWrite.test.ts`
- `commands_tests` BLE rename 契约读 coordinator runtime include

### B. 真机代理冒烟（已有）

见 `20260727-owner-proxy-hardware-smoke.evidence.md`：

- 当前 debug 二进制 + `Blistener` 1.0.3 BLE OTA **preflight PASS**
- CLI 听写 BLE 管线 PASS（无人说话空转写为预期）
- 有线 COM3 工厂包 check PASS

### C. 版本

`package.json` / `tauri.conf` / `Cargo.toml` / 固件 `VERSION` 均为 **1.0.3**。

## 发布前仍须完成的工程步骤（包）

1. **用当前 master 打 Type MSI**（`scripts/windows-package-msvc.ps1` 或项目约定打包命令）  
2. 将 **MSI + 固件 OTA zip** 放到 **Denzic 根** 为：  
   - `ListenerType_1.0.3_x64_en-US.msi`  
   - `ListenerFirmware_1.0.3_ota.zip`  
3. 跑：  
   `npm run check:release-root-artifacts -- --type-source <新msi> --firmware-source <zip>`  
4. （建议）同版本 MSI 安装替换一次 smoke  
5. 可选：preproduction human-review 弹窗（产品手感；机器 BLE 管线已覆盖主干）

## 已知非阻塞点

- 无人说话时 ASR 空转写：预期  
- 旧 `releases/1.0.3` 包日期早于 master：勿直接当「当前源码发布物」  
- dirty-inventory 若仍有 pending_release_validation 分类路径，以 hygiene 脚本结果为准

## Go / No-Go

- **机器门禁：GO（当前树）**  
- **对外分发：NO-GO 直到重新打包并把根目录包哈希对齐**  

一句话：**代码可以按 1.0.3 发；包还要用当前树重打并放到 Denzic 根。**
