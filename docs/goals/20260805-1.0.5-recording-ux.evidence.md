# Evidence: Listener 1.0.5 录音体验升级

> 日期：2026-08-05  
> 会话：Grok section 第四次接管（修唤醒空体秒结束 + 重装）  
> 状态：**机器闭环 PASS**；owner 人工仍待复测  
> Owner 备注 triage：`docs/release/evidence/1.0.5-recording-ux-20260805-142628/owner-notes-triage.md`  
> 本刀证据：`.artifacts/listener-ux-wake-no-body-20260805/`
>
> ### Grok 第四次（本条）
> - 根因：session `72519330` 胶囊后 ~1.2s `target_speaker_inactive_1000ms` 空体 →「没有识别到语音」；owner 会话 `3e6672b7` 口述抱怨自动结束
> - 修复：自动唤醒且**正文未开始**时结束阈 = **3.0s**；正文开始后仍 1.0s/长文 2.0s
> - `node scripts/verify-listener-1.0.5-protected-contracts.mjs` → 1.0.4 baseline + **11** 个 1.0.5 门 **PASS**（含 `wake_no_body_uses_longer_abandon_timeout`）
> - 单测：`automatic_wake_no_body…` / endpoint 族 / `default_done_message…` **PASS**
> - 安装身份：Program Files EXE SHA256 `70293BFC…04DD2DF` = 1.0.5，与 MSI 载荷一致

## 1. 版本身份

| 项 | 值 |
|---|---|
| 产品版本 | `1.0.5`（package.json / Cargo.toml / tauri.conf.json） |
| 安装路径 | `C:\Program Files\Listener Type\listener-type.exe` |
| 安装 EXE SHA256 | `70293BFCD10950F5231C47C9246604C6837F7AA6C38C5F5B8EA483A0704DD2DF` |
| MSI | `<release-root>/ListenerType_1.0.5_x64_en-US.msi` |
| MSI SHA256 | `9154CA37E9FCA64329A2AE8AB0F3962C7E015EF24B7092E2507BC92275DCB8AA` |
| MSI 时间 | 2026-08-05 18:27 |
| 进程 | `listener-type` 已从 Program Files 启动（本刀装包后） |

## 2. 机器门

```text
node scripts/verify-listener-1.0.5-protected-contracts.mjs
→ 1.0.4 baseline PASS + 1.0.5 contracts PASS (10 gates)
```

关键单测（`cargo test --lib`）：

| 测试 | 结果 |
|---|---|
| `long_form_endpoint_requires_two_seconds_without_that_speaker` | PASS |
| `default_done_message_treats_raw_insert_after_polish_failure_as_successful_fallback` | PASS |
| `target_speaker_endpoint_requires_one_second_without_that_speaker` | PASS |

## 3. 装机用户路径日志（本机已发生）

来源：`%LOCALAPPDATA%\Listener Type\Logs\listener-type.log` + `capsule-timeline.log`

| 指标 | 样本 | 判据 |
|---|---|---|
| stop→Transcribing | `stop_to_transcribing_ms=1`（session `29ed7e9d…`） | ≤150ms PASS |
| 句末结束 | `timeout_ms=2000 sentence_pause=true long_form=false` | 句末 2s 合同保留 |
| stop→Done | `stop_to_done_ms=281` / `291` | 可观测 PASS |
| 失败可感知 | 胶囊 Done：`已粘贴原文（仍在剪贴板，可 Ctrl+V）` | C2 文案 PASS |
| 预览保留 | recording → transcribing 仍带正文 | A3 不干等空态 |

说明：样本中 `polish_failed=true` 属环境润色凭证问题；**插入原文 + 剪贴板提示**路径按 1.0.5 合同工作。

## 4. 已落地能力清单（相对 Goal）

| Goal 项 | 状态 |
|---|---|
| 默认 1.0s 本人结束 | 保留 + 契约 |
| 长文听写 2.0s（默认关） | `long_form_dictation` + 设置开关 |
| 句末自动 2s（。！？） | 已实现（owner 确认保留） |
| stop 即 Transcribing | `request_embedded_audio_stop_feedback` 前置 |
| 「正在听」/处理中文案 | i18n + 自动化区 |
| 中途不掐防回归 | pending 挡结束 + provisional 刷新时钟 + 契约门 |
| 上屏/失败说清楚 | Done 文案含剪贴板引导 |
| stop→Done 可观测 | `stop_to_done_ms=` 日志 |
| 1.0.4 防回退 | baseline 契约全 PASS |

## 5. 未完成 / 人工边界

- **Owner 手测**：短句 1s 收、长文开关、别人说话不拖、中途不掐体感。
- **可选 P1（未做，不挡本 Goal 机器闭环）**：忠实/整洁文案再产品化、自动标点可关、灯效与胶囊严格对齐。
- **工作树**：1.0.5 改动仍在 `Listener-Type` 未提交；勿在未确认前 reset。

## 6. 结果

`result=PASS machine_closed_loop`  
人工验收前请用上述 Program Files 1.0.5 身份，不要用 target/debug。
