# Goal：1.0.3 打包并发布到 Denzic 根

> Active Goal。完成前不得宣称「可对外安装包已就绪」。

## 目标

用 **当前 master HEAD** 重打 Type MSI，与已发布的 Firmware 1.0.3 OTA zip 一并落到 **Denzic 根** 标准文件名；跑 root artifact 校验；可选本机同版本安装 smoke；可选 GitHub Release。

## 成功标准

| # | 检查 | 自动判定 |
|---|---|---|
| 1 | `scripts/windows-package-msvc.ps1` 全量构建（禁止 `-ReuseExistingExe`，因 product 提交新于旧 MSI） | MSI 落盘且版本 1.0.3 |
| 2 | 复制 MSI + `ListenerFirmware_1.0.3_ota.zip` → `C:\Users\Billy\Desktop\Denzic\` | 文件存在 |
| 3 | `npm run check:release-root-artifacts -- --type-source <msi> --firmware-source <zip>` | exit 0 |
| 4 | （建议）`-InstallMsi` 或 msiexec 同版本安装 + 安装目录 exe 哈希对齐 | 命令成功 |
| 5 | 证据 `docs/goals/20260727-1.0.3-publish-packages.evidence.md` | 文件存在 |

## 人工边界

- 固件侧有未提交改动时 **不重打固件 zip**，沿用已验 1.0.3 OTA 包（preflight 已 PASS）。
- Authenticode 签名若本机无证书：记录为内部/未签名包限制（SmartScreen）。
- GitHub Release：有 `gh` 权限则建 `v1.0.3` 并上传 MSI；无权限则写清阻塞。

## 机器闭环

输入：当前 Type HEAD + 既有 firmware OTA zip。输出：根目录包 + SHA256 + 校验 JSON。
