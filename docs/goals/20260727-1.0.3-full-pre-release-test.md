# Goal：1.0.3 发布前全量测试（先测后发）

> Active Goal。完成前不得宣称「可对外安装包已就绪」。

## 目标

在 **当前 master 工作树** 上跑完 Type 发布相关机器测试与已有真机探针；全部 PASS 后给出 **打包/发布 GO/NO-GO**。不改产品语义，只修测试/门禁/环境阻塞。

## 成功标准

| # | 检查 | 自动判定 |
|---|---|---|
| 1 | `npm run release:check` | exit 0 |
| 2 | `npm run check:module-budgets` + softOver4500 空 | exit 0 |
| 3 | `npm run check:embedded-ble-processing-led` | exit 0 |
| 4 | `cargo test --lib -- --test-threads=1`（全量 lib） | 0 failed |
| 5 | `npm run verify` + `npm test` | exit 0 |
| 6 | 当前 debug 二进制：OTA package check + BLE preflight + runtime smoke | 命令 exit 0 / 证据 JSON |
| 7 | 证据落盘 `docs/goals/20260727-1.0.3-full-pre-release-test.evidence.md` | 文件存在且列结果 |

## 人工边界

- 不要求 owner 弹窗 human-review（机器代理冒烟可替代主干）。
- **不自动上传商店/写 GitHub Release**；打包若跑则只做本地 MSI，是否拷根目录写在证据里。
- 本机 OOM/锁文件导致的失败：记录限制与重试，不装 PASS。

## 机器闭环

输入：当前 HEAD。输出：各命令 exit code + 日志路径。任一 FAIL → 修到绿再标 Goal PASS。
