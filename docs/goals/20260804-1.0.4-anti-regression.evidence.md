# Listener 1.0.4 防回退工程证据索引

## 交付物

| 项 | 路径 |
|---|---|
| Goal 合同 | `docs/goals/20260804-1.0.4-anti-regression.md` |
| ACTIVE_GOAL | `../ACTIVE_GOAL.md`（Listener 根） |
| 需求→测试映射 | `scripts/listener-1.0.4-requirement-test-map.json` |
| 一键回归 | `scripts/run-listener-1.0.4-regression.ps1` |
| 身份检查 | `scripts/verify-listener-1.0.4-identity.ps1` |
| 保护指标合同 | `scripts/verify-listener-1.0.4-protected-contracts.mjs` |
| 干净环境门 | `scripts/verify-listener-1.0.4-clean-env.ps1` |
| 一键诊断 | `scripts/collect-listener-1.0.4-diagnostics.ps1` |

## 复用（不重复造框架）

- Type：`release-check.mjs`、`run-v1.0.2-regression.ps1`、`check-type-startup-reconnect-speed.ps1`、`check-ota-transfer-speed-log*.mjs`、`run-installed-ota-hidden.ps1`、`tools/collect_ai_diagnostics.ps1`、`cargo test --lib`
- Firmware：`release_check.ps1` 子集静态脚本、`verify_listener_103_ota_regression.py`、`verify_voice_recording_control_fsm.py`、`verify_audio_ble_upload_long_session.ps1`、`collect_ai_diagnostics.ps1`

## 运行

```powershell
# 默认：静态/源码/身份/干净环境（不装包、不刷机、不碰生产数据）
pwsh -NoProfile -File Listener-Type\scripts\run-listener-1.0.4-regression.ps1

# 硬件全门（设备已装 1.0.4 且已配对）
pwsh -NoProfile -File Listener-Type\scripts\run-listener-1.0.4-regression.ps1 -IncludeHardware

# 诊断包
pwsh -NoProfile -File Listener-Type\scripts\collect-listener-1.0.4-diagnostics.ps1
```

## 机器结果（本会话）

### 静态 / 源码门

| 项 | 结果 |
|---|---|
| 一键回归（无硬件） | **PASS** `.../20260804-104659/summary.json` |
| 保护合同 8/8 | PASS |
| 身份 | PASS（冻结 MSI/OTA/tag） |
| 干净环境 | PASS |
| Firmware 静态 | PASS |
| dictation 单测 | PASS（110） |

### 硬件门（本机已装 1.0.4 + listenerB）

权威汇总：`Listener-Type/.artifacts/listener-1.0.4-regression/hardware-continue-20260804-105752/hardware-gates-summary.json` → **`PASS_WITH_NOTES`**

| 门 | 结果 | 证据 |
|---|---|---|
| 温启动 notify-ready | **PASS**（样本 925/988/1017/885/958 ms 均 <3000） | 见 summary；OTA 三连后曾短暂 3.8–6.7 s，恢复后仍 <1 s |
| OTA 连续三次 | **PASS** 65.1 / 63.5 / 61.5 KiB/s，recoveries=0，fixed≈5.3 s | `.../ota-triple/triple-summary.json` |
| ≥20 min 录音长稳 | **PASS** 1202 s、52 周期、Type 存活、0 WDT/crash/丢包信号 | `.../type-soak-20min-r4/long-soak-summary.json` |

说明：固件 `verify_audio_ble_upload_long_session` 单会话会因无近场语音被 auto-stop 提前结束；产品长稳采用 Type 持 BLE + 串口短开 `VREC` 周期切换，墙钟 ≥20 min。

### 其他

| 项 | 结果 |
|---|---|
| npm 全套 | PASS（frontend tests + production build） |
| 诊断包 | `Listener-Type/.artifacts/listener-1.0.4-diagnostics/` |
| Type 全量 Rust | PASS（1018 passed，4 ignored） |
| 1.0.4 MSI | PASS（`A7DFA087076DF7691ACB33E78360D16A9D01EB8AACB1FA437EC32FF46DB5E26F`） |
| 安装身份 | PASS（Program Files payload 与 MSI 行政解包 payload 均为 `F4F25718450C16ADD658602875B3AF26F674DE57D054AFD3DB16499E3E2A1B98`） |

## 未覆盖风险

1. OTA 三连后温启动曾出现 >3000 ms 瞬态（已记入 notes）；若 owner 要求“任意时刻严格 <3000”，需单独根因。
2. 单会话连续 20 min 不静音 auto-stop 的声学耦合长录未做（被 Type 周期浸泡替代）。
3. 人工边界已由 owner 在本轮自然使用中确认当前体验可发布；新胶囊呈现仍以安装包实用观察为最终边界。
