# 本人正文恢复与低能量尾部修复（2026-09-13）

状态：候选迭代中，未达到发布标准。该文记录本轮失败和取舍，不将源码测试当作实机通过。

## 本人正文恢复

旧的全段 speaker-split recovery 要求所有云端说话人分段都支持本人；一旦后来出现旁人，前面有本地证据支持的本人正文也无法恢复。按真实 59e1f309 的时间和分数形状构造的完整 ASR 解析测试，旧逻辑输出只剩“开始录音”，期望包含本人正文；英文变体使用同一规则。RED 见 task work/owner-prefix-before.log。

当前改动逐段保留已验证的本人前缀：要求云端稳定、有起止时间、顺序不重叠、本地窗口仍保留本人、不存在该时间段的旁人否决。遇到不满足证据的段落停止恢复；不跳过旁人去恢复后面文字。复用原来的短片段旁人否决与同一说话人编号排除，不调相似度门槛，不增加语句特例。

B40C8F88 候选还尝试收紧原始预览发布。七轮实机结果为正文 2 pass / 4 fail / 1 unverified，音频交付 6/7 完整（未产出会话一轮不认定通过）。长句 77973d8c 和停顿 e1b2736e 正文完整；背景后唤醒 5bde89e4 有开头误识别；自动短句 b538df38 未产出正文；手动短句重复“你”；短句接旁人 789b9256 缺“你”；长句接旁人 ebd2401d 在本人还说话时截断。

实机失败：`target_speaker_inactive_no_body_3000ms` 在约 8.3 秒完成时只剩前 23 字。此前将其归因于“正文已到但预览被过滤”不成立：进一步把 trace 与 STOP 对齐，STOP 前只有“开始”唤醒词碎片；“今天。”在 STOP 后约 161 ms 才到。自动结束早于正文识别到达，需查启动等待和 provider 处理进度，而不能凭非空字符计数改变端点。两处预览收紧已撤回，保留独立的逐段正文恢复。不能把截断后的无旁人尾句认作抗干扰成功。详见 task outputs/no-body-endpoint-evidence.md。

## 低能量尾部不能当作说话人变化

直接逐窗口对齐而非仅看总体 RMS：

- 215bacee：最后四窗口 peak_rms=181.6–184.9，score=0.113–0.149；身体语音基准 0.664。旧逻辑判 degraded=true。
- ee4dba0a：最后四窗口 peak_rms=188.3–193.7，score=0.045–0.079；身体语音基准 0.649。旧逻辑也判 true。
- 这些窗口已经低于现有身份模块的 512 peak RMS 可靠门槛，身份分类本来是 Uncertain；后续漂移检测却丢掉了信号质量信息，拿底噪相似度与本人有声片段直接比较。
- 当前云端终帧已到、TSE 后续仍在处理尾部。按照已有质量条件做离线反事实计算，两轮 degraded 都回到 false；这只证明应修质量传递，速度改善仍需新安装实测。

证据：task outputs/quiet-tail-window-evidence.json，脚本 work/audit-quiet-tail-windows.py。各窗口由同分数、单次分析和 0–300 ms 内的下一 ASR 观察唯一匹配，歧义时脚本报错。旧日志没有直接附带质量标记，新代码在同一条 audio_end_ms 观察中记录该标记。

已实现并安装验证：从 SessionSpeakerObservation 将信号质量沿 coordinator 传到 ASR，按同一 audio_end_ms 保存；排除原始前两个唤醒窗口后，再从分数漂移计算中忽略不可靠窗口。沿用 real_speech>=1000ms 和 peak_rms>=512 条件，不更改声音输入、身份判定、自动结束保护、物理残差处理或持续 NonTarget 规则。质量记录与同段身份历史共同限长、重置和跨传输恢复。

9A3147CB 候选七个固定正例：3 pass / 3 fail / 1 unverified；音频交付 6/7 可核对完整，余下一轮多会话歧义。停顿长句正文完整，末次预览增长至完成 2773 ms（353 同素材 4057 ms）。clean 观察窗口包含两个完成会话，原汇总保留歧义；仅对首会话 90772d76 的机制分析显示不再等待底噪 TSE，STOP→完成 408 ms，不能据此算正文通过。短句手动/短句旁人尾音通过；自动短句丢正文；背景后唤醒错字和合并重复；长句旁人尾音仍泄漏。无口令负例约 46 秒未开正式会话或输出，但未覆盖登记本人匹配无口令。

## 后续：过滤后的覆盖范围不能遗失

9A3147CB 的 96da8904 trace 显示：frame 32 云端修正版进入后，合并候选出现旧滚动分支重复；frame 37 的 speaker_filtered_result 已去掉旁人，但 merged_candidate 又保留重复正文/旧尾段。代码中的最终“不可缩短已显示内容”随后保住了错误分支，不能只修终稿末端。

新候选针对时间范围交接：当既有旁人判定允许排除尾段，且完整云端修正版覆盖旧时间轴、选中内容是它的连续前缀，使用完整云端时间覆盖证据替换被修订的旧分支。要求完整时间、无重叠、选中前缀与原始分段一致；未覆盖的早期正文、缺时标、非连续片段、本人分段例外继续保留原规则。中文/英文行为回归旧代码会追加旧段，RED 记录在 work/covered-owner-merge-before.log。实机是否改善须以新安装候选重新回放为准。

## 验证范围

- 模型离线对照见 task outputs/speaker-window-quality.json；本次不换 CAM++/WeSep，不把单份录音分数当成用户准确率。本人 1200/2000ms 两段互有重叠，0.86 相似度不是独立录音泛化验证。
- 保留所有候选及失败。新候选继续同样七个固定答案场景，另测无口令负例。
- A 的开头、E 的误识别/重复、G 的真实旁人/重叠，以及 F 的首预览都仍需要联合验收；不把本轮结束等待优化当作所有体验问题解决。
- 真正的商业发布还须连续三轮场景、真实双人、物理键、重连、USB/电池和升级回退验收。用户不需要记事本，最终输出与实际插入的验证范围分开记载。
