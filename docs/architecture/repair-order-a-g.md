# A–G 整体规划（锁）

禁止「你说 C 就改结束钟、你说 D 再改回来」。
C 和 D 是同一口钟；F 和 G 是同一层说话人判定。单症状补丁会循环。

不改 UI。不拧全局灵敏度。手动右 Ctrl / EC11 对照不许变差。

## 冻住（不再跟手改）

| 合同 | 内容 | 反例 | 冻结于 |
| --- | --- | --- | --- |
| **C + D 结束钟** | `OwnerContinuing` 不停。正文未出且本地还在说话时，3 秒空等不停。正文已出且融合 Quiet，允许 1 秒停。1 秒这个数不动。 | 说完约 1 秒必须停；确认旁人可以停 | `9343203` / 已装 `5FC3E333…` |
| **E 上屏** | 自动结束打胶囊最后预览 | 上屏 = 预览 | 已装 |
| **F 预览** | 已显示的字只增不减 | 不以整句过滤稿腰斩 | 已装 |
| **G 干扰（听写默认）** | 旁人不续钟；不承诺 100% 隔离。宁可多字不吞半句 | 主人换气不断（C） | 部分；不再拧过滤开关 |

单报「提前结束」或「停不了」**不够**改结束钟。必须同一版里口令 3 和 4 都失败，才允许动结束层，且一次改完、成对测。

## 下一刀（唯一允许继续的结构活）

**A 自动唤醒：词在前，不是再加确认窗口。**

- 现在：VAD 截 4～6 秒再找词 → 慢、间歇失败。fail-fast 已装，只是少占废窗口。
- 目标：连续音频上听当前唤醒词，命中再开录。
- 反例 **B**：不说唤醒词不开录。

未做完这刀之前，自动唤醒不当唯一入口。

## 验收口令（同一版都要过）

1. 连说三次「开始录音」能开录（A；未达前可按键）  
2. 不说唤醒词不开录（B）  
3. 中间换气不断（C）  
4. 说完约 1 秒自己停（D）  
5. 上屏 = 胶囊最后预览（E）  
6. 预览过的主人字终稿不得变少（F）  
7. 电脑放旁人：不把结束拖住；不保证旁人一句都没有（G 听写默认）  

## 明确禁止

- 为 C 加宽「新鲜人声」否决，导致 D（已发生：379 次 ignore）  
- 为 D 拆掉 OwnerContinuing，导致 C  
- 为 G 用整句标签覆盖已显示预览（F）  
- 为 A 再切第四段 PCM 窗口  
- 整体重写 coordinator  
