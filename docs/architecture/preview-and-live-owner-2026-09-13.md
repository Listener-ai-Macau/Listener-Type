# 预览延迟与现场本人验收 · 2026-09-13

## 13:41 更新：保留正常体验，修复已结束候选的多余 STOP

当前安装 Type 1.0.5 SHA-256 `008367353BA6DCBFD29DEB07639222171D254209D33319FEAD623BCCCEA43435`；MSI `.artifacts/repair-20260913-terminal-reject/ListenerType_1.0.5_x64_en-US.msi`，SHA-256 `098CEB6618B4FBDE686DE19742247D236710BA39327B2E5B007572ED463F66DA`。固件保持 `d592fff3e2e09bd0`。运行核对 PASS，调试录音已关闭，流式输入与非 TSF 回退已恢复，9222 已关闭。未公开发布，A–G 未全部通过。下方早期章节的运行身份是历史快照。

用户本轮要求优先保持当前稳定性和体验。没有改变声纹阈值、2500 ms 正文结束策略、预览加速配置或固件。

- 实机 `4fd65348` 只剩“开始”：旧隐藏候选 `2558853272` 已于 05:15:39.707 UTC 收到设备 STOP，迟到的本地拒绝又于 05:15:41.451 派发裸 STOP，误停下一段 `2558853273`。主机直到 41.582 才看见新 START，故写入前的主机身份检查不能消除此竞态。现在终止/取消路径明确标记物理段已结束，仅关闭自己的主机候选，不再排队 STOP；仍在录音的拒绝候选保留停止动作。行为测试同时覆盖终止、下一候选未观察到、迟到旧结果和活跃候选拒绝。
- 尾句过滤补齐“唤醒词与长正文同属一个云端稳定行”的正文校准；排除该行开头最多 1800 ms 的唤醒范围，只用后续正向正文证据。对不同稳定说话人的短行，允许最多一个 1200 ms 分类窗口的后续证据，仍要求连续低分且无本人正向窗口。不同语言、未校准正文、本人续说和过期证据均有回归。此分支尚不能认定实机抗干扰验收通过。
- 全量 Rust 1325 通过、31 忽略、0 失败；cargo check、前端构建、brand/cloud/traceability 与打包通过。首次测试因既有源码字符串断言不兼容 CRLF 失败；该断言现规范化换行后重新全量通过，未降低语义条件。

所有下列测试为外放 → 实际键盘收音 → BLE → 已安装 Type → 最终输出。测试期间关闭了光标回退和流式插入，因此不作为真实上屏验收。首预览时间从整个素材开始计算，包含唤醒词，不能等同正文起声延迟。

| 场景 | session | 首预览 ms | 最后预览到完成 ms | 结论 |
| --- | --- | ---: | ---: | --- |
| terminal-reject-clean-1 | 63828219-24b4-4159-88a8-b51d44b31cc1 | 4227 | 2763 | body_complete |
| terminal-reject-clean-2 | 34579194-50ff-45e2-86f2-d82ce3cb4b96 | 4190 | 4533 | body_complete |
| terminal-reject-clean-3 | fcbc9229-eced-4b56-a6ac-8c66979b16d9 | 4337 | 2715 | FAIL_owner_body |
| terminal-reject-long-other | 80a7e7b2-66b2-431f-a118-aa1f5f1bef9a | 3844 | 2769 | FAIL_foreign_tail; local_and_cloud_both_classified_other_as_owner |
| terminal-reject-next-command | e04284e0-eaa2-49d2-98c3-2494faeb7b9e | 4625 | 2738 | FAIL_owner_ASR; empty_cloud_final; absence_of_next_command_is_not_acceptance |
| terminal-reject-other-1 | c65af62d-77e3-4781-8a63-ab4f2e8f19dc | 7449 | 3141 | FAIL_missing_owner_first_word; foreign_tail_absent |
| terminal-reject-overlap | 无完成会话 | — | — | FAIL_wake_segment_boundary; redundant_STOP_skipped |
| terminal-reject-pause-1 | 5f5f9f7e-67bf-4d04-a203-08d99a0fadd9 | 4616 | 2727 | body_complete |
| terminal-reject-pause-2 | 361789c7-80a8-4484-ad2f-8418f86577ca | 4245 | 2646 | body_complete |
| terminal-reject-pause-3 | 90a89be4-e0cc-4a71-99f1-7d36d117579d | 4228 | 2672 | body_complete |

明确保留的失败：

1. 无效语音紧接唤醒词：终止候选不再补发 STOP（3283–3286 四次明确日志），但设备 max-duration 分段与尾包排空期间错过了唤醒，整轮失败。不能靠此次 STOP 修复宣称背景中唤醒已解决。
2. 历史 G 素材 `c65af62d` 无旁人尾句，但漏“你”。同一实机 WAV 的本地离线识别含“你”，说明至少不能把云端/最终输出丢字误报为原始麦克风没收见；仍需核对云端与过滤边界。
3. 本人下一指令素材 `e04284e0` 未带入下一指令，但正文错字和漏词，云端最后 `two_pass_empty=true`、无稳定 speaker id。过滤新分支没有该行证据，不能据此宣称自动结束或 G 通过。
4. 清晰长正文加另一种合成声音 `80a7e7b2` 保住全部正文，却带入整个背景尾句。云端三行全部标为 speaker 0，本地背景多个窗口也超过 Target 阈值 0.55。这是身份误判，不能仅靠延时或统一阈值解决；扬声器重放不替代两名真人现场验收。

后续顺序：先处理背景语音中的唤醒分段空窗；定位本人首字/正文误识别的云端边界；以真实不同说话人验证身份误判，再调整会话声纹证据来源；最后缩短无效结束等待。完整 A–G 多轮、本人/旁人重叠、实际按键输入、电池拔线及升级链路未完成，继续阻塞发布。全部原始日志和失败保留在任务目录 `work/terminal-reject-*`。

## 12:55 更新：现场尾句与自动结束仍阻塞发布

最新安装 Type 1.0.5：`26970018FFDC0FB99A0F7BD3E797B0B5C17B554BBA68E6435AA2D4C6A54AD8DD`；固件仍为 `d592fff3e2e09bd0`。MSI 位于 `.artifacts/repair-20260913-pending-identity-final/ListenerType_1.0.5_x64_en-US.msi`，SHA-256 `EA02676B69DDE68294B15FDD3FD72403AE8B86459EDCF0D564500B576F1A95AB`。已安装并通过运行路径/版本/哈希核对，不能据此称为发布验收通过。

本轮现场基线：

- `136896b3`（12:31）：本人提问之后追加“怎样的明天”。1834/1834 音频包收到，唤醒登记声纹匹配成功。没有保存现场 WAV。正文声纹多数为 Uncertain，不能通过收紧阈值删除正文。
- `327d8e7a`（12:38）：本人“你要告诉我要说什么话”后追加多句粤语背景声；保存了现场 WAV。不能把此例当作先前合成 G 素材通过所覆盖的场景。
- `dfa2041c`（12:40）：用户确认本人说“我先检查键盘连接，然后再确认语音输入，最后，然后这句话说完。”，输出随后追加“等我过去咯，快快”。主机于 04:40:46.247 UTC 自动请求 STOP，04:40:46.499 完成。该轮光标输入被测试设置关闭，不能误报自动结束失效或实际输入验收通过。

新候选增加的是临时预览等待：已有正文后出现新的未确认身份句子，同时出现信号合格的身份差异或连续低分窗口时，暂缓新尾句的原始预览和相应端点续期；初始正文不受限制。稳定说话人结果/本地正向身份可解除等待，不将等待写成永久删除，不修改声纹阈值。登记声纹未匹配、但口令已接受的会话也适用。三项行为回归涵盖全部预览回调、最终本人续说恢复、初始正文和过期证据；最终测试二进制全量 1323 通过、31 忽略、0 失败。前端构建、brand/cloud/traceability、ASR 预览静态检查与 cargo check 均通过。

安装后实机结果（均为外放经键盘重新收音，不能等同首次现场声音）：

| 素材/会话 | 实际结果 | 判定 |
| --- | --- | --- |
| `dfa2041c.wav` → `7b7d2524` | 没有原背景尾句；正文成了“把键盘连接……这句话是我”；首预览 4102 ms，最后增长到完成 3381 ms | 首尾识别失败；没有观察到新等待分支触发，不能归功于修复 |
| `327d8e7a.wav` | 隐藏候选未通过唤醒，没有完成记录 | A 未通过，G 无验收结论 |
| 连续长句原素材 → `4f26f37c` | 原 80 字正文完整，追加“小爱同学，开空调”；首预览 4341 ms，最后增长到完成 3031 ms | 用户确认追加句是本人随后发出的另一条指令，应归为结束太晚，不归为旁人 G 失败 |

长句素材时长 20.86 s，播放开始 04:53:04.346 UTC，完成 04:53:30.680，原素材结束后约 5474 ms 才完成。云端把新增指令分为另一说话人，现有连续正文恢复规则仍将其纳入预览/最终。不能简单降低统一 2.5 s 等待：此前该类改动会在本人持续说话时截断；下一步需把有完整正文且识别已追齐的收尾，与未识别完的停顿续说区分，并复测两者。

前两次测试脚本因短 WAV 结束得早，固定 20 s 等串口不足，脚本报错；串口随后正常写出 `serial_closed` 后才补做汇总。脚本已改为从串口启动起计算 55 s 总截止时间，避免按素材长短调整等待。保留原失败日志，没有将采集中途当作通过。

现场采集和外放已结束。`recordAudioForDebug=false`、`allowNonTsfInsertionFallback=true`、`streamingInsert=true` 已恢复，经应用 `--quit` 正常退出后正常启动，关闭 CDP 调试端口，运行核对 PASS。未公开发布，A–G 未全部修好。原始记录在任务工作区 `outputs/live-tail-baseline-20260913.json`、`work/pending-identity-*`。

---

状态：继续修复，不能宣布 A–G 全部通过。本文接续 `repair-evidence-2026-09-13.md` 的早期候选，旧失败保留。用户要求直接核对最终输出，后续不再打开记事本或测试文本框。

## 已安装的通用修复

- Type 的 DeferredAsrBridge 建连前只保留最后 300 ms，导致一次真实会话丢掉 2.3 秒前缀。现在完整保存、按顺序交付；回归覆盖建连中继续到达的音频与重入交付。
- 固件把小包的主动让步和媒体速率限流重复计时，实机吞吐只有约 28.2 KB/s，低于 32 KB/s PCM，结束后还要排空约 5.6 秒。让步计入同一速率预算后，实机约 34.2 KB/s；实际 C 实现的 30 组小包与混合包回归验证速率与有界让步。
- 手动录音缺少云端说话人回调时，结束钟现在读取继续到达的本地音频快照，避免将云端停更当作用户已经安静。
- ACTIVATE 附近旧隐藏候选的 MaxDuration STOP 不再因已经有少量正文就结束整个会话；新物理段绑定到同一逻辑会话并接管。用户主动 STOP 保持有效。最新实机完整正文通过，但尚未再次强制复现该边界竞态，不能以此替代竞态验收。
- Windows TSF 重试的两条退路现在也遵守 `allowNonTsfInsertionFallback=false`。旧代码曾在关闭回退后仍发送 80 字输入操作，已告知用户，未删除用户草稿。

## 真实收音对照

| 会话 / 场景 | 实际结果 | 延迟与边界 |
| --- | --- | --- |
| 42c5e6f2，前缀修复 + 旧固件节奏 | 连续合成外放，完整 80 字实际输入 | 首预览 4968 ms，最后预览到完成 8501 ms；发送积压明显 |
| ad4dfd1f，旧 Type + 新节奏 | 失败，只剩 7 字 | ACTIVATE 和旧 MaxDuration STOP 竞态；保留原失败 |
| 0a92b1c2，旧 Type + 新节奏，手动正文 | 失败，仍说话时截止 | 旧云端快照导致本地语音被误判过期 |
| e6e9411c，新结束状态修复，手动正文 | 收全 22.32 秒，但最终仅 38 字且不准确 | 云端前两句二遍结果为空；本地离线识别有完整正文。DMA 溢出 2880 字节，不能声称所有测试均为零 |
| e516989c，当前 BBB10AC8 Type + d592fff3 固件 | 连续外放最终 80 字完整 | 首预览 4508 ms，最后预览到完成 2875 ms，传输约 34208 B/s；DMA=0、BLE 缺包=0 |
| ccf451e4，旧本人片段 + 另一音色尾句 | **G 失败**，正文起始恢复，但整段旁人尾句仍进入最终结果 | 外放拼接对照，不能当作两位真人现场验收 |
| 532ef20a，Billy 现场自然说话 | **开头识别错误**，后半句完整，自动停止成功 | 10.36 秒音频，1067 包全部收到；“我现在在说”被识别为“他在说” |
| cd49e3e7，现场验收准备前的另一会话 | **未通过**，无有效正文，约 60 秒后设备上限结束 | 终端声纹接受但没有口令文本；低电平噪声持续刷新本地语音时间。不能计为本人说话通过 |

现场本人期望：“我现在在说一句话，然后你帮我看一下这句话行不行？我随便说了一句话，哈”。Listener 最终：“他在说一句话，然后你帮我看一下这句话行不行？我随便说了一句话，哈”。直接对同一 WAV 做云端原始响应检查，原始结果已经为“他在说”，不是最终拼接才替换成这个词。

## 云端定位，不等同于实机验收

相同手动失败 PCM 两轮对照：`end_window_size=500 / force_to_speech_time=0` 的前两个二遍分句无文本，终稿 39 字；`1000 / 1000` 返回完整 81 字。第二轮首回调 8667→2386 ms，最大回调间隔 5359→2351 ms。还需在其他声音上核查，不能据单个素材修改产品门槛。

现场本人 WAV 的云端原始结果将同一个人的唤醒和正文分成 speaker 0 / 1；不带本地声纹的诊断路径只保留唤醒，不能将该路径的结果当成已安装自动唤醒链路。原始回包与经过筛选的结果分别保留。

参考：[火山流式识别官方文档](https://www.volcengine.com/docs/6561/1354869)。分句参数和说话人聚类版本的对照仍在进行，尚未据此宣布识别、抗干扰修好。

## 版本与测试方式

该表最新安装 Type 1.0.5 exe SHA-256：`BBB10AC882FF0AF13504E5892399B481989C269BFA7731801A946165A62EDF86`。MSI：`.artifacts/repair-20260913-preview-delivery/ListenerType_1.0.5_x64_en-US.msi`，SHA-256 `3B4F1B349B80A08EB0581B42F3943197EF17EB6CD6D58C776CC7AC0972B7538D`。

固件 build_id：`d592fff3e2e09bd0`，1701200 字节，SHA-256 `01d62ffb6d0489e0bc487e167436767c1ffdd6458fca29bb1625a67ce8a98407`。BLE OTA 31161 ms，重连 1005 ms，确认成功。

用户要求仅检查最终输出后的测试临时关闭 `streamingInsert` 和 `allowNonTsfInsertionFallback`，打开 debug 录音。对应历史 `insertStatus=failed` 表示没有向前台应用输入，正文仍从历史读取，不算输入链路验收。测试结束必须恢复原设置。一次外放无会话是设备已按原设置进入一分钟低功耗，不计 ASR 通过或失败。

当前还有新胶囊会话保留旧结束提示的问题；修复按会话归属清除旧消息，保留同会话实时预览及兼容的无 session 音量事件，待安装验证。

本机完整证据位于任务工作区 `outputs/preview-and-live-owner-evidence.json` 与 `work/`。真实旁人现场、电池拔线、原竞态再现仍未完成。用户原有未提交工作保留，无公开 Release。

## 11:50 后续定位与已安装分句候选

采用 `end_window_size=1000`、`force_to_speech_time=1000`、显式 `ssd_version="200"`。同一手动失败 PCM 三轮均从 39 字恢复完整 81 字；另一完整外放录音的 PCM 原始全文没有退化，最大回调间隔约 3.4→2.4 秒。当前本人短句原始回包仍为“他在说”，聚类仍把唤醒和正文分为 0/1，不能声称该项或 G 已解决。

已安装分句候选 exe `66586AE0CDC0ECB5F39E39FBFEA0990A34C6B9A5938C3ED84B65732E3E6741D6`，MSI SHA-256 `D274B19883E88695515E92D7E21E04312F311F99716799C89C07EC983308C535`，目录 `.artifacts/repair-20260913-segmentation`。

| 实机重放 | 结果 |
| --- | --- |
| 8c1637be，原手动失败素材 | 最终完整 80 字，首预览 1891 ms，最后预览到完成 3510 ms，DMA=0、缺包=0 |
| 2e5379cf，原连续自动素材 | 正文完整但末尾多“和”，**失败保留**；首预览 4298 ms，末预览到完成 2689 ms，DMA=0 |
| 3362b898，相同连续自动素材重复 | 没有“和”尾字，但第一句被客户端重复，**失败保留**；首预览 4594 ms，末预览到完成 4381 ms，DMA=960 字节 |
| 06b47471，原本人片段 + 旁人尾句 | 旁人尾句未进入最终，但本人开头多“你”，不能记为整体通过；首预览 7215 ms，末预览到完成 3549 ms |

胶囊旧提示修复已装入此候选：手动失败提示结束、回到 idle 后，下一自动录音从空消息开始，未继承上轮提示。前端完整回归通过。旧延迟静态脚本还在寻找已移除函数，且将测试源码当成生产实现；现改为检查实际 visual-only helper 和统一预览 reducer，保持禁止 visual-only 提交正文的检查。

3362b898 的原始 PCM 没有重复。对同一录音取得原始云端响应：前几次 `utterances` 没有起点、words 时间戳；3200 ms 文本“今天的工作顺利是先”；3500 ms 第一次带逐词起点 1080 ms，并修订为“今天的工作顺利，是先完成”。旧 merger 在第一次有时间戳时跳过了无时间戳修订窗口，把前缀追加两遍。修复在没有旧 timed ledger 时保留活动窗口修订关系，并开始使用新的 timed ledger；已存在时间戳的后续拼接维持原路径。中英文标点变化、已确认前缀保留和真正分离的新句子行为回归通过，待最新安装复测。

## 12:00 最新安装与复测（当前状态）

Type 最新安装 exe SHA-256：`C6403A3EB2761894D976ED4FD182C1B5EDE9D3B12E15B0CBE8063832321357E4`；MSI：`.artifacts/repair-20260913-first-timing/ListenerType_1.0.5_x64_en-US.msi`，SHA-256 `A8AFBCA03A057F3A8C2B983BAF2008D659ABCFC016637881A8A1253E42E55364`。固件仍为 `d592fff3e2e09bd0`，未改硬件收音或声纹阈值来配合本次拼接修复。

| 最新实机 session | 最终输出判断 | 预览与结束 |
| --- | --- | --- |
| 49ad8e1b，原连续自动素材 | 没有重复前缀，但“在提交”变成“再提交”，“可追溯原因”少“溯”；准确性未完全通过 | 首预览 4667 ms，末预览到完成 3501 ms；DMA=0 |
| 018bfd39，把 3362b898 失败实机 WAV 再外放收音 | 没有重复前缀，末尾有“可追溯原因”，但开头少“今天”；不能当作全文正确 | 首预览 5240 ms，末预览到完成 3730 ms；此例是二次声学回放 |
| 4c33d7d5，原本人片段 + 旁人尾句 | **此素材完整通过**：最终“你帮我看一下，现在感觉好像是越来越差了。”；无重复、无旁人尾句 | 首预览 7542 ms，末预览到完成 3577 ms；未取得此例 DMA 汇总，不虚报为零 |

一次素材通过不能覆盖之前 G 的失败，也不能代替两位真人现场插话。直接检查 Listener 历史得到最终结果，最后两例 `copiedFallback`、其余 `failed` 均属于临时结果检查模式，未验证向用户应用输入。

对 Billy 现场 WAV 另做首字加速 0/10 对照，云端原始终稿均为“他在说”，首回调约 2022/1995 ms；开头错误未修好。当前待确认口令与正文的实际衔接方式，不按某一固定句子添加纠错。

最新完整 Rust 回归：1320 passed / 0 failed / 31 ignored；cargo check、前端 build、全部前端回归、Windows 静态脚本、品牌、云配置、traceability 和单一 ASR 预览链检查通过。完整发布验收仍未通过：开头误识别、偶发多字、结束延迟波动、无正文噪声续录及真实拔线/现场旁人场景继续保留。

已通过安装版 IPC 恢复并读回 `recordAudioForDebug=false`、`streamingInsert=true`、`allowNonTsfInsertionFallback=true`。组合恢复/强制重启命令曾被自动审批以 `blocked by policy` 拒绝；随后仅恢复设置，并通过应用原生 `--quit` 正常退出后重启成功，未强杀进程。调试端口 9222 不再监听，正常运行 gate PASS（主进程 20588、helper 21380）。没有打开记事本或测试文本框，没有公开发布，用户未提交改动保留。
