# 语气词标点修复与实机对照

当前已安装 Type 1.0.5，SHA-256 `1E71341BD26EF1D60D9F7D002EBA725A0F0C8017AC490C9D58F2087A31E72648`；键盘仍为 `d592fff3e2e09bd0`。MSI 在 `.artifacts/repair-20260913-filler-diagnostics/ListenerType_1.0.5_x64_en-US.msi`，SHA-256 `8578C6AB6DCC26092B2AAEDBE498ACF938EB6C33F843A05301C4857AE593C4DE`。

用户询问时 `removeFillerWords=false`。本次修复后已开启；日常流式输入及回退恢复，调试录音关闭，9222 调试端口关闭。没有进行固件升级或稳定渠道发布。

## 产品改动

删除语气词后的分隔符不再只取首个标点：保留句末标点、引号、括号、换行及问叹号组合，仅合并删除犹豫词造成的停顿分隔冗余。无语气词的文本不额外清理标点。最终仲裁路径验证了开关关闭保持原文、开启去词保留断句。

同时新增受现有调试录音开关控制的 ASR 分阶段内存记录，每条限512个Unicode字符、总512条，明确记录截断与丢弃数。结束后归档同会话文字、PCM长度和最终输出；删除录音/历史时连带清理，也清理无WAV的孤儿记录。日常关闭时不保存这些诊断文字。

## 安装版真实外放结果

下列结果只验证键盘收音后的最终历史输出；测试期间未验证实际光标输入。所有证据在任务目录的 `work/filler-diagnostic-*`，不是代码测试模拟出来的结果。

| 场景 | 实际结果 | 结论 |
| --- | --- | --- |
| 正常80字长句，9ef77392 | 正文完整，末次字数增长至完成2936ms | 本轮正文通过，延迟未达探索目标 |
| 含嗯/呃的手动短句，f324a50a | 正文19字完整但无标点 | 标点验收未通过；云实时文字已无标点，最后`two_pass_empty`，从会话记录保留正文；不能归咎于去词删除 |
| 原漏首字录音，51e64e6e | “你帮我看一下，现在感觉好像是越来越差了。”且无旁人尾句 | 本轮通过；未复现原丢字，不能宣称已根治 |
| 同一短句无口令 | gate Reject，无正式输出 | 本轮负例通过 |
| 同一短句前加口令，0aa64735 | gate Accept；“嗯”被识别成“文”，最终正文含额外“文” | 唤醒通过，正文失败；禁止硬删“文”使测试过关 |
| 旧真实背景对话 | 外放前已有dc96fcb5听写在运行 | 本轮作废，不能算误唤醒或通过；统计已标`session_started_before_playback` |

新增诊断在实机上能正常归档：正常长句229条、原漏字录音62条，均无容量丢弃。原漏字重放这次云原文已带“你”，不能把一次识别变好当作新修复的因果证据。

## 验证与后续

语气词相关6项Rust检查、诊断默认关闭/Unicode限额及存档清理2项检查通过；cargo check、前端构建、brand/cloud/traceability及MSI构建通过，安装payload与运行进程身份核对通过。判定器7项、会话关联4项检查通过。未重新宣称全量Rust或A–G全部通过。

采集脚本新增播放前空闲检查；统计除按session隔离，还检查会话开始是否早于播放，保留手动测试自己启动的短窗口。旧日志只含字数增长，仍不可代替最后一次实际文字变化或正文起声计时。

下一优先级是联合验证无口令与有口令，继续定位E识别错误、C/D结束及G旁人混入。代码中仍存在“terminal且声纹匹配，即使无口令证据也接受”的旧保唤醒兜底，这与严格无口令不输出存在冲突；不能只删除兜底而忽略已知麦克风监听/预录空窗，否则会再引入漏唤醒。新固件候选尚未安装，原工具升级命令曾被策略拒绝。A–G尚未解决，不能放行稳定发布。
