# 审核通过后交给 Luna 的指令

本文件供用户批准方案后使用；文件存在本身不构成批准。

将下面整段发给 Luna：

```text
我已审核并批准 Listener 录音体验与发布执行方案 v2，请开始按方案执行。

仓库：C:\Users\Billy\Desktop\Denzic\Listener\Listener-Type
方案：docs/architecture/commercial-recording-controller-plan-2026-09-14.md
进度：docs/architecture/luna-recording-execution-status-2026-09-14.md
历史实测目录：C:\Users\Billy\Documents\Codex\2026-09-12\c-users-billy-desktop-denzic-listener\work
固件仓库：C:\Users\Billy\Desktop\Denzic\Listener\Listener-Firmware

先读 AGENTS.md、完整方案和进度文件。第一次执行从 P0 开始；恢复执行先核对现场，然后从第一个未通过的任务卡继续。保留全部现有未提交修改，不把旧 handoff 草稿当作已验证实现。

每次只执行一张任务卡，通过门槛、写入证据后自动继续下一张，不需要我反复说“继续”。尚未通过的门槛不能跳过；确实不依赖该门槛的工作可先继续并记录。

每次修改说明一条可验证的原因与预期结果；验证必须调用真实生产决策，保留失败，不能靠改断言、固定中文特例或调整统计区间过关。相同原因两次修正仍失败时，整理证据交审核者判断，不继续盲试。

不要自行开子代理，不扩展到技能清理、首页重写或依赖升级。只读取当前卡需要的资料。完整构建在 P7，实机在 P8；不要边改参数边反复安装。

遵守最后本人有效语音/预览后 1 秒结束的合同；按方案明确区分普通停顿、疑似换人、身份未完成，不能造一个 NonTarget 时间戳掩盖不确定。最终文本和实际插入分别验收。

报告使用方案模板，清楚区分代码通过、音频模型评测通过、安装实机通过和真人现场通过。需要本人或第二人配合时具体说明只缺哪项，同时继续独立工作。发布材料准备到可审核；没有明确外发授权不要推送稳定更新或公开发布。
```

执行时可用的状态询问：

```text
请只告诉我：当前 P 几、已通过哪些门槛、最新真实失败是什么、下一步如何验证。继续当前任务，不重跑已经有效通过的检查。
```
