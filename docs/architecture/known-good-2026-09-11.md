# 已知可用状态（2026-09-11）

Owner 验收通过。这是当前体验报告应对照的 Type 包。后续改动不得静默改掉下面合同。

## 包

- Git：`220e7fd`（`use/1.0.5-good`）
- 已装 exe：`C:\Program Files\Listener Type\listener-type.exe`
- SHA-256：`FDE19997C64081D7DA33BAC845BA08980F5F96EE786B919FF2AEF31B13559F7F`
- MSI：`Listener-Type/.artifacts/windows-msvc/ListenerType_1.0.5_x64_en-US.msi`
- MSI SHA-256：`2709EE079014350777CC57EE7FD775A06C1DD56DCA4ECBACD538C4188F9B30CE`
- 固件未改：`1.0.5` / `5304d27a2a93a2c9`

## 通过时实际跑通的路径

会话 `2678d958` / 唤醒 `2293892314`（约 06:30Z）：

- 第 3 秒本地 `ExactStart` 听清「开始录音」并 live Accept
- 胶囊预览 1→75 字跟着涨
- 上屏 76 字（唤醒词 81→76 剥掉，隔离 `filter_required=true` 也没有把正文裁空）
- 结束是用户停，没有 1 秒掐句

Owner 原文上屏多次连说仍有字，没有再变空。

## 不可回退合同

优先级仍是：保住唤醒 > 停不了 > 不吞你的字 > 预览顺。

1. 有词（ExactStart / PresentLater / 关键词）即使声纹第一次没对上也开录。
2. 窗口结束时声纹已认出主人、本地没听清口令，仍开录并新开一段正文录音。
3. 胶囊已经显示的字是上屏下限；隔离不得封成 Empty。
4. 唤醒词剥离不得把长正文剥成空。
5. 正文结束钟是最后一次出字后再等 2.5 秒；句号不等于说完。
6. PCM 空包不得冲掉预览。
7. 窗口结束时正在跑的 3 秒/5 秒确认要从此刻再等 1.2 秒，不得用 250ms 预算丢掉。

## 仍未根治（先不放开，以免打回可用）

- **口令和声纹都没命中的窗口仍会拒。** 通过之后 `2293892315` 起多次 `phrase_signal=None` 且 `enrolled_non_match`。再接受这类窗口会明显增加误唤醒，会把当前最好体验打回去。
- 终端离线 KWS 补听以前 500ms 就放弃，实际要 ~1.5s 才跑完。可把等待加到 1.2s（与 inflight 确认相同），但仍要求本地确认口令；不把「谁都不是」的窗口改成接受。
- KWS 即使在成功的 `ExactStart` 上也经常 `kws_hit=false`，唤醒目前主要靠本地确认，不靠关键词模型。
- 识别用字（「三点」写成「3点」等）是云端 ASR，不在这次主机合同里改。
- 低优先：误唤醒、确认旁人的新尾巴不加（但已显示的字仍不回撤）。

手动右 Ctrl / EC11 对照不许变差。
