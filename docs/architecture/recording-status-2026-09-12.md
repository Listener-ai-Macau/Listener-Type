# 录音现状（2026-09-12 重记）

对照 A–G。Type 当前包 `8A997434…`（`cc1fd32`），固件 `4c11128`（重叠再开窗已撤回）。

## A 唤醒

- **还没根治。** 4 秒能量窗口有时套住「开始录音」，有时套不住。套住：约 2 秒 live 亮。套不住：像没唤醒。
- **已堵的洞：** 结束包丢失后生命周期卡在 Active，后面每次开始录音都被拒（`拒绝新的唤醒候选`）。空闲时解开这把锁。
- **试过撤回：** 拒了马上重叠再开窗 → Type 被拒窗打满、云端超时、胶囊空。已刷回 0.4 秒冷却 + 0.42 秒确认。

## B 误唤醒

未放开「口令和声纹都没有也开录」。

## C / D 结束钟

成功进门后：开口句 2.5 秒，句号不当说完。宁可挂着，不 1 秒掐句。

## E 吞字

成功进门后：胶囊字是上屏地板。结束时迟到正文不丢。`开始。` 当唤醒残段剥掉。

## F 预览

成功进门后：PCM 空包不冲字。去语气词不边出边抠。预录不再整段塞进云端（只留约 0.3 秒）。重叠再开窗曾把预览堵死，已撤回。

## G 抗干扰

低优先。隔离不能把已显示的字裁空。旁人新尾巴可以没有。

## 合同

进门成功时看：live 约 2 秒、预览跟着涨、上屏≈胶囊。差在进门不要去拧结束钟。
